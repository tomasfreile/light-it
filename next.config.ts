import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['res.cloudinary.com', 'cloudflare-ipfs.com', 'static3.depositphotos.com', 'example.com'], 
  },
};

export default nextConfig;
